kind:
  __description: This object defines the different compute resource kinds
  __example:
    "baremetal"
  type: string
  constraints:
    - oneof:
        - baremetal
        - virtual_machine
        - container
        - cluster
        - virtual_cluster
