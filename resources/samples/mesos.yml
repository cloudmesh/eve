mesos-docker:
    id: mesos-docker
    container:
        docker:
            image: mesosphere/inky
            credential:
                principal: my-principal
                secret: my-secret
        type: MESOS
    args:
    - argument
    cpus: 0.2
    mem: 16.0
    instances: 1
