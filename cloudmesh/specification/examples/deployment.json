{
    "deployment": {
        "cluster": [{ "name": "myCluster"},
                    { "id" : "cm-0001"}
                   ],
        "stack": {
            "layers": [
                "zookeeper",
                "hadoop",
                "spark",
                "postgresql"
            ],
            "parameters": {
                "hadoop": { "zookeeper.quorum": [ "IP", "IP", "IP"]
                          }
            }
        }
    }
}
